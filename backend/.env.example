# Path to the SQLite database file
DB_PATH=./ergostudy.db

# Set to "none" + true when the frontend runs on a different domain (HTTPS)
COOKIE_SAMESITE=lax
COOKIE_SECURE=false

# Frontend origin (used for the root endpoint info)
FRONTEND_URL=http://localhost:3000
